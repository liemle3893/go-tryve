version: 2

project_name: autoflow

before:
  hooks:
    - go mod tidy
    - go test ./...

builds:
  - id: autoflow
    main: ./cmd/autoflow
    binary: autoflow
    env:
      - CGO_ENABLED=0
    ldflags:
      - -s -w -X main.version={{.Version}}
    goos:
      - linux
      - darwin
      - windows
    goarch:
      - amd64
      - arm64

archives:
  - id: autoflow
    name_template: "autoflow_{{ .Version }}_{{ .Os }}_{{ .Arch }}"
    format_overrides:
      - goos: windows
        format: zip

checksum:
  name_template: "checksums.txt"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"

release:
  github:
    owner: liemle3893
    name: autoflow
  draft: false
  prerelease: auto
